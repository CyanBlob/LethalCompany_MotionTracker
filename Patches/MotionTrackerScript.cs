using UnityEngine;

namespace LC_MotionTracker.Patches;

public class MotionTrackerScript : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        Debug.Log("MOTION TRACKER");
    }
}